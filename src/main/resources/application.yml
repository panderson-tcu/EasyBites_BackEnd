# This defines which application properties will be active.
spring:
  profiles:
    active: dev # by default, active profile is dev. Environment variable in azure will override
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
api:
  endpoint:
    base-url:
server:
  port: 80

